# With USB: device uses adb reverse, so localhost works.
# Without USB (Wi‑Fi): use your PC's LAN IP so the phone can reach the API.
EXPO_PUBLIC_API_URL=http://localhost:4000
# EXPO_PUBLIC_API_URL=http://192.168.1.241:4000
